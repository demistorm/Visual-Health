name: Publish Mod

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Gradle
        run: ./gradlew build

      - name: Read versions from gradle.properties
        id: version
        run: |
          echo "mod_version=$(grep 'mod_version=' gradle.properties | cut -d'=' -f2)" >> $GITHUB_OUTPUT
          echo "minecraft_version=$(grep 'minecraft_version=' gradle.properties | cut -d'=' -f2)" >> $GITHUB_OUTPUT


      - name: Publish Fabric
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: jAEjiIKx
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: 1439096
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          version: ${{ steps.version.outputs.mod_version }}-fabric
          name: VR Swing Sprint (Fabric ${{ steps.version.outputs.minecraft_version }}-${{ steps.version.outputs.mod_version }})
          files: fabric/build/libs/!(*-@(sources|javadoc|dev|shadow)).jar
          loaders: fabric
          github-token: ${{ secrets.GITHUB_TOKEN }}
          changelog-file: CHANGELOG.md

      - name: Publish Forge
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: jAEjiIKx
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: 1439096
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          version: ${{ steps.version.outputs.mod_version }}-forge
          name: VR Swing Sprint (Forge ${{ steps.version.outputs.minecraft_version }}-${{ steps.version.outputs.mod_version }})
          files: forge/build/libs/!(*-@(sources|javadoc|dev|shadow)).jar
          loaders: forge
          github-token: ${{ secrets.GITHUB_TOKEN }}
          changelog-file: CHANGELOG.md

      - name: Publish NeoForge
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: jAEjiIKx
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          curseforge-id: 1439096
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          version: ${{ steps.version.outputs.mod_version }}-neoforge
          name: VR Swing Sprint (NeoForge ${{ steps.version.outputs.minecraft_version }}-${{ steps.version.outputs.mod_version }})
          files: neoforge/build/libs/!(*-@(sources|javadoc|dev|shadow)).jar
          loaders: neoforge
          github-token: ${{ secrets.GITHUB_TOKEN }}
          changelog-file: CHANGELOG.md
